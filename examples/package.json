{
  "name": "examples",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify index.js -t babelify -o bundle.js",
    "publish": "npm run build && echo 'react-sprite-scaled-animator.surge.sh' > ./CNAME && surge ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babelify": "^10.0.0",
    "browserify": "^16.5.1",
    "react": "^16.13.1",
    "react-dom": "^16.13.1"
  },
  "devDependencies": {
    "surge": "^0.21.3"
  }
}
